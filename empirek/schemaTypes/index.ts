import {post} from './post'
import {author} from './author'
import {service} from './service'
import {category} from './category'
import {project} from './project' // Optional, if using separate gallery items

export const schemaTypes = [post, author, service, category, project]
